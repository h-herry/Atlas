# Atlas Common — 测试环境配置 / Test environment configuration
# version: v1.2.401

debug: true

spring:
  datasource:
    url: jdbc:mysql://mysql-test:3306/atlas?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: atlas_user
    password: ${MYSQL_ROOT_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.atlas: INFO
