# Atlas Common — 开发环境配置 / Development environment configuration
# version: v1.2.30

debug: true

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/atlas?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_ROOT_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.atlas: DEBUG
